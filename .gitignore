node_modules
dist
*.tsbuildinfo

# Playwright MCP test artifacts (snapshots, console logs, screenshots)
.playwright-mcp/

# Playwright e2e output (traces, reports) — outputDir is relative to the config
client/test-results/
client/playwright-report/

# local env / secrets — never committed
.env
.env.*
!.env.example

# MCP config carries an API key — commit the sanitized .mcp.json.example instead
.mcp.json
!.mcp.json.example

# cortex graph cache (CTXS-10) — a disposable generated artifact, never committed
.spec/.cache/
.vscode/project-links.json
THOUGHTS.md
