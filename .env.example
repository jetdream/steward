# Copy to .env for local development (.env is gitignored).
# The dev datastore is the docker-compose Postgres + pgvector (DEC-36):
#   docker compose up -d
DATABASE_URL=postgres://steward:steward@localhost:5432/steward
