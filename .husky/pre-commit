#!/usr/bin/env sh
# The full gate must pass before any commit (DCX-14 + DEC-37 gate-first):
# docs graph, then strict types, then lint (no-any). Nothing lands red.
# docs-check engine = the pinned cortex dependency, no vendored copy (ADR-0001).
npx cortex-docs-check && npm run typecheck && npm run lint
