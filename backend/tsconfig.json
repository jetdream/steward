{
  "$schema": "https://json.schemastore.org/tsconfig",
  "//": "Server package: Node globals + the ES2023 lib. Type-checked directly by the gate (npm run typecheck). Includes drizzle.config.ts so the migration config is checked too.",
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2023"],
    "types": ["node"]
  },
  "include": ["src", "drizzle.config.ts"]
}
