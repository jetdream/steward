{
  "$schema": "https://json.schemastore.org/tsconfig",
  "//": "Minimal for the scaffold increment. Node types + the server lib set join with the walking-skeleton increment (DEC-37 deliverable 5), which also splits the gate into project references so each root gets its own lib/types.",
  "extends": "../tsconfig.base.json",
  "include": ["src"]
}
