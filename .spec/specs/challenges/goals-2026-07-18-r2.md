---
kind: challenge-record
spec: goals register — compass-coverage rework (DEC-11 / INC-2 honesty pass)
round: 2
date: 2026-07-18
verdict: pass
by: cortex:architect-challenger (COMPASS LENS — altitude / coherence / completeness)
---

# Goal-coverage rework challenge — round 2 (compass lens)

Delta-scoped re-challenge after the r1 fail. Both r1 findings verified fixed; the
fixes survive adversarial re-derivation. One fresh one-line-fix low; per the
convergence rule (no surviving high) this converges to **pass**, and the low's
wording sharpening was applied in the same change that records this verdict.

## Verbatim verdict

VERDICT: pass
SPEC: /home/coder/project/.spec/product/goals.yaml

FINDINGS:

Prior-finding verification (both hold):

- [r1 high — coherence] RESOLVED. The critical-path rule is now stated in the register
  `intent` (rule 1) and each status is re-derivable from it. Re-derived all five
  independently from the `serves`-edge sets and the rule reproduces every status line:
  G-2 (path generate→approve, measured at approve; GEN+APR+AUT pinned, PUB legitimately
  downstream of the measurement point → complete); G-3 (subject = in-app time, produced
  by the founder-facing surfaces/automations APR/UX/DS/PRO/AUT/MEM/ONB/BOT/CHT/INT, all
  pinned → complete); G-1 (value-delivery path runs through the loop, pins PUB but omits
  GEN+APR → partial); G-4 (live-post cadence, pins GEN+PUB but omits the APR/AUT approve
  step → partial); G-5 (full loop, pins none of GEN/APR/PUB → partial). The "cross-goal
  reliance in prose" escape hatch is gone — G-1/G-4 are now `partial` on the same basis
  that downgraded G-5, so the two-standard incoherence is eliminated. Verified on the
  live graph: G-1/G-4/G-5 report "coverage not complete" + "Q-12 open (blocks
  completion)"; G-2/G-3 report coverage complete (NOT-achievable only for unbuilt
  specs). DEC-11 records rule 2b and names the reversed r1 attempt; INC-2 resolution
  records the 3-partial/2-complete outcome; Q-12 now `binds: [G-1, G-4, G-5]` with
  per-goal candidate edge closures (GEN-1+APR-1+PUB-1→G-5; APR-1+AUT-1→G-4;
  GEN-1+APR-1→G-1) — all mechanically correct against the existing `serves` edges. Gate
  green, 0 errors.

- [r1 low — altitude leak] RESOLVED. The only surviving `$199/mo` is in G-1's
  `statement` (the metric definition itself, which correctly belongs to the goal), not
  in any coverage reasoning. No `(P1)`/`(P2)` priorities and no `±5 min`-class
  parameters remain in the reasoning; requirement IDs are cited by causal role only.

Fresh attack on the changed sections — one non-blocking note:

- [low] The rule's phrase "the chain of capabilities whose output the metric directly
  depends on" is slightly looser than the operative test the reasonings actually apply
  ("produces the metric's subject"). Under a strict value-sensitivity reading,
  draft-generation quality (GEN/STR) moves G-3's edit time — G-3's own prose concedes it
  "leans downstream on ... high draft quality (G-2)" — which a careless future reader
  could use to argue GEN belongs on G-3's path and G-3 should be partial. Does NOT
  survive as blocking: the register resolves it correctly and consistently — G-3 files
  that dependency under the EMPIRICAL threshold (rule 2 — whether the <15-min target is
  *hit*), not under subject production (rule 1 — whether in-app time as a measurable
  subject *exists*, which the pinned surfaces fully produce). Every status is derivable
  under the consistent "produces the subject, up to the measurement point" reading.
  Improvement only: sharpen the rule wording so the value-vs-subject line is explicit.
  [APPLIED in this change: rule 1 now reads "…the chain of capabilities that produces
  the metric's subject, up to its measurement point (a capability that only moves
  whether the TARGET is HIT — not whether the subject exists — is a rule-2 empirical
  dependency, not a rule-1 path step)."]

Convergence: no surviving high; the single low was a one-line wording clarification,
applied in the recording change → PASS.
