---
kind: challenge-record
spec: .spec/specs/str-posting-strategy.yaml
round: 2
date: 2026-07-19
verdict: pass
by: cortex:architect-challenger (delta-scoped — verify r1 fixes + independent dual-home sweep)
---

# STR spec — round 2 (delta)

All five round-1 findings verified RESOLVED. Independent exhaustive dual-home
sweep CLEAN: section (c) is single-homed in Memory across glossary Redirect,
EXP-19 v2, APR-3 v2, STR-4 v2, CHT-2 v2, PIPE-1 v2, goals, and DM-3 v2; the only
old phrasings live in the superseded experience/ui.yaml and historical challenge
logs (both retained by design). TOP-4 integration coherent (section (a) is soft
editorial preferences drawn from the agenda, not a second store of the topic
list). No surviving high.

Four one-line precision notes, applied in the change recording this verdict:
- STRS-4 "deterministic" qualified to routing INDEPENDENCE (the org overlay never
  gates GR-1..GR-6), NOT detection completeness (each GR check's detection is a
  probabilistic classifier — LRN-20); acceptance made differential.
- The How-I-write section-(c) edit routes through the MEMS-1 write path as a
  founder-stated rule (the confirmed-redirect funnel) — the MEMS-1 caller-list
  straggler closed spec-side without re-opening the approved MEM spec.
- STRS-2 acceptance re-keyed by content class (soft editorial vs org-rule/
  prohibition), not by box.
- STRS-1 section (a) framed as soft editorial preferences drawn from the agenda,
  explicitly not a second copy of the topic list.

VERDICT: pass
