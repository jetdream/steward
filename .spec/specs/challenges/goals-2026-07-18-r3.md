---
kind: challenge-record
spec: goals register — Q-12 closure + outcome-tier adoption (DEC-13)
round: 3
date: 2026-07-18
verdict: pass
by: cortex:architect-challenger (COMPASS LENS — altitude / coherence / completeness)
---

# Goal-coverage rework challenge — round 3 (compass lens)

Delta-scoped re-challenge after DEC-13: the Q-12 capability gaps were closed
(core-loop `serves` edges wired to G-1/G-4/G-5; four requirements → v2; coverage
re-pinned; G-1/G-4/G-5 flipped partial→complete) and the cortex outcome-validation
tier was adopted (`outcome: in-progress` on all five goals, gating `achieved` on a
field measurement). Verdict **pass** — no surviving findings; this converges the
challenge.

## Verbatim verdict

VERDICT: pass
SPEC: /home/coder/project/.spec/product/goals.yaml

FINDINGS: none survived scrutiny — this converges the challenge.

What I attacked and why it held:

1. Serves-edge closures — correct and on-path. Verified on the four bumped
   requirements: GEN-1 → [G-1,G-2,G-4,G-5], APR-1 → [G-1,G-2,G-3,G-4,G-5], PUB-1 →
   [G-1,G-4,G-5], AUT-1 → [G-2,G-3,G-4], all v:2 / decided-by DEC-13. Each added edge
   sits on the target's critical path. Probed the sharpest potential incoherence —
   G-1/G-4/G-5 pin only GEN-1 while G-2 pins GEN-1..6 — and it holds under the stated
   rule: for the loop-running goals the subject is a post existing/live/paid (GEN-1
   produces the draft, minimal chain), whereas G-2's subject is a draft good enough to
   approve unedited, so GEN-2..6 are on G-2's path and are target-movers (outcome, not
   path) for the others — exactly the rule's "moves whether the target is hit, not
   whether the subject exists" clause.

2. `against` re-pins complete and consistent. Item-count reconciliation of every
   goal's `against` vs its true serves-edge set matches exactly (G-1: 21, G-2: 38, G-3:
   46, G-4: 28, G-5: 20). Every changed ID at @2; zero stale @1 references to
   GEN-1/APR-1/PUB-1/AUT-1 anywhere in .spec; the v0.20.1 npm toolchain gate reports
   288 IDs, 1546 refs, 0 errors, 0 warnings, all pins fresh.

3. Coverage complete is now earned, not asserted. G-1/G-4/G-5 flipped partial→complete
   only because the loop is pinned on each critical path; no remaining unpinned
   critical-path capability. The GEN-3 picture blocker is correctly classified an
   outcome risk (mitigated by R-3), not a decomposition gap.

4. Outcome tier is honest and structurally resolves INC-2. All five carry outcome:
   {status: in-progress} — none falsely validated (no cohort exists). The engine
   reports "outcome not validated" and gates `achieved` on it, so no goal can report
   achieved on build alone — the structural replacement for the DEC-11 prose caveat,
   closing the exact overstatement INC-2 opened. in-progress blocks correctly carry no
   against/evidence (not a completion claim), well-formed under GRP-1.

5. No regression in G-2/G-3, no mis-status. G-2/G-3 coverage unchanged except the
   mechanical @1→@2 re-pins; both remain correctly complete. The r2 wording low was
   adopted — the intent now reads "produces the metric's subject, up to its measurement
   point" with an explicit target-vs-subject carve-out.

Non-blocking operational note (not a defect in this delta): the Claude Code PLUGIN
hook runs an older cortex build that doesn't recognize outcome/format-version and lints
.spec/.cache, yielding false-positive red in-session. The authoritative pre-commit/
pre-push npm toolchain is v0.20.1 and green, so the spec content is sound — realign the
plugin build to the v0.20.1 pin (or exclude .spec/.cache from the hook scope) as
toolchain hygiene; does not affect this verdict.
