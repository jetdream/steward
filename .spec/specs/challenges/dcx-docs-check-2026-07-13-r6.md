---
kind: challenge-record
spec: .spec/specs/dcx-docs-check.yaml
round: 6
date: 2026-07-13
verdict: fail
by: architect-challenger (via general-purpose agent, delta-scoped)
---

# Verbatim verdict

VERDICT: fail
SPEC: .spec/specs/dcx-docs-check.yaml
FINDINGS:
- [severity: high] DCX-15 v1's rule is false in an `implemented` spec — the harness does not "apply each acceptance mutation": six acceptance clauses have no case (four gate error paths: DCX-2 definition-outside-owning-file, DCX-2 prefix-claimed-by-two-files, DCX-2 prefix-claim-under-unregistered-prefix, DCX-11 non-accepted-ADR in constrained-by; two report clauses: DCX-6 uncovered-P0 assertion, DCX-10 effective-serves — `--json` is never run by the harness at all) — survives scrutiny because for DCX-15 the harness IS the behavior, making this the round-1 high class (spec-vs-implementation drift inside the lint's own spec), and it is load-bearing now: the spec and specs/CLAUDE.md direct pre-push and delta-scoped re-challenges to trust the harness "instead of re-deriving mutations by hand", so a regression in those four gate error paths would ship green; it also leaves DCX-6 and DCX-10 with zero test citations (grep confirms no case name cites them), undercutting the implemented-status flip's ≥1-test-citation claim under the per-ID reading — fix: add the six cases to scripts/test-docs-check.mjs (each ~2 lines; all six behaviors verified fireable by my mutations: MEM-99 in risks.yaml → "defined outside owning file"; `prefix: [R, G]` → "prefix G already owned by"; `prefix: [R, QQQ]` → "claimed but not registered"; proposed ADR-0001 cited in constrained-by → "only accepted ADRs may constrain"; clean-run assertion on the coverage report; one `--json` case asserting MEM-1 inherits `serves: [G-2, G-3]`), which simultaneously repairs the DCX-6/DCX-10 test-citation gap.
- [severity: medium] DCX-3 v4's challenge-record exemption is self-granted and location-unrestricted — any .spec/**/*.md declaring `kind: challenge-record` plus four trivial frontmatter fields becomes a fully unlinted reference channel (verified: .spec/product/evil-note.md containing ZZZ-999, MEM-999, and stale pin `G-1 v99` → exit 0, and its citations also vanish from cascade lists) — survives scrutiny because the rationale only justifies evidence records under .spec/specs/challenges/, and the abuse is one frontmatter block, not an edit to any curated file — fix: one line in the rule and in scripts/lib/docs-graph.mjs:206 — exemption applies only to files under .spec/specs/challenges/; a challenge-record kind elsewhere is an error.
- [severity: medium] DCX-13 v2's "under .spec/specs/challenges/" clause is unenforced — pointing the spec's `challenge.record` at a copied record at .spec/product/smuggled-record.md satisfies the gate (verified: exit 0; scripts/docs-check.mjs:139 does `files.get(join(ROOT, ch.record))` with no path-prefix check) — survives scrutiny as rule-text-vs-implementation divergence that also lets records escape the challenges/ append-only convention — fix: one line, error unless `ch.record` starts with `.spec/specs/challenges/`.
- [severity: medium] A fail verdict is still forgeable to pass: the cross-check compares block↔frontmatter only, never the evidence — editing the r5 record's verbatim body line `VERDICT: pass` → `VERDICT: fail` while frontmatter and block say pass passes the lint (verified: exit 0) — survives scrutiny because the laziest realistic forgery is pasting a real fail record and flipping the wrapper metadata, the agent definition pins a machine-checkable `VERDICT: pass | fail` output line, and DCX-13's own motto is "a verdict without evidence is not a verdict" (wholesale fabrication stays protocol-scoped per the round-5 precedent, which is why this is medium not high) — fix: one line, require the record body to contain a line `VERDICT: <frontmatter verdict>`.

Attacks that held: round-5's two non-blocking fixes are real and harness-covered (case 15 unclaimed-prefix, case 24 non-comment-line gate match — both green); the harness ran 28/28 on a clean tree and its DCX-13 negative paths (record missing, wrong kind, frontmatter verdict mismatch) all fire; DCX-4's new schemas fail closed (a YAML doc claiming kind challenge-record hits unknown-kind; learnings type/scope and approved-block shapes are harness-tested); DOCS_CHECK_ROOT fails loud on a bogus root (exit 1, ENOENT), is child-env-scoped in the harness (no parent leak; harness stays 28/28 even with the var exported in the parent), and no hook or config sets it; DCX-1..14 code citations exist (Implements headers in scripts/docs-check.mjs and scripts/test-docs-check.mjs); tree restored byte-identical after every mutation (79/79 checksums, git status unchanged).
