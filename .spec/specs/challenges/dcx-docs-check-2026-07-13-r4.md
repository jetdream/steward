---
kind: challenge-record
spec: .spec/specs/dcx-docs-check.yaml
round: 4
date: 2026-07-13
verdict: fail
by: architect-challenger (via general-purpose agent)
---

# Verbatim verdict

Restoration verified byte-identical against baseline checksums. All angles exercised; two findings survived self-refutation, two accompany as non-blocking. Final verdict follows.

VERDICT: fail
SPEC: .spec/specs/dcx-docs-check.yaml
FINDINGS:
- [severity: high] The ADR namespace has no enforceable owner, so a phantom ADR defined as a YAML items-map entry satisfies the DCX-11 design gate — verified by mutation twice: an `ADR-0099` item with `status: accepted` added inside `.spec/product/goals.yaml` (no prefix claim at all) and in a new file claiming `prefix: ADR` both produce exit 0 while `constrained-by: [ADR-0099]` on an approved spec passes DCX-11, with no real decision record existing. It survives scrutiny because it is an accident channel, not just malice: this repo itself models inline item definition (the DCX spec), so an agent told to "record ADR-0099" plausibly writes it as a register item; the resulting citation is indistinguishable from a legitimate one in review; and DCX-2 v3's own rule text ("an ID definition outside its prefix's owning file is an error"; "a phantom namespace must not enter the graph") is violated — the registry declares ADR's owning file as `adr/NNNN-slug.md`, but the owners map in `scripts/docs-check.mjs` (DCX-2 block, lines 24–40) is built only from YAML `prefix:` claims, so `owners.get('ADR')` is undefined and the check silently skips. Same defect class as round 3's high (gate bypass), and the superseding-cascade mechanism DCX-11 promises breaks entirely for such items. Fix: in DCX-1/DCX-2 (and `buildGraph`/check code), make filename-defined namespaces exclusive — an items-map key or `prefix:` claim under `ADR` is an error; additionally require `defs.get(entry).kind === 'adr'` in the DCX-11 ADR branch; bump DCX-1/DCX-2 and add the mutation to `acceptance`.
- [severity: medium] DCX-11's path branch accepts any approved YAML as a design constraint — verified by mutation: `constrained-by: [.spec/product/goals.yaml]` on an approved spec exits 0, though the rule says "architecture doc path". `scripts/docs-check.mjs:116-118` checks only existence and `status === 'approved'`, never `kind === 'architecture'`, so a cross-cutting spec can satisfy its non-empty constrained-by with a requirements register or another spec (including itself). Not high on its own: the cited artifact is real, reviewed, and approved; the wrong-kind path is greppable and visibly odd in a diff; and status regression of the cited file still cascades. Fix: one-line kind check (`target.data?.kind !== 'architecture'` errors), bump DCX-11, add acceptance mutation.
- [severity: low] The `acceptance` section was not extended for the error classes added in the round-3 bumps — DCX-2 v3 phantom definitions/claims, DCX-3 v3 unregistered-prefix tokens and exact-name TEMPLATE narrowing, DCX-7 v2 unexpected extensions, DCX-4 v4 frontmatter parse-error propagation all lack a listed mutation, so re-running the acceptance list would not catch a regression of exactly the round-3 fixes. Non-blocking: the item rules normatively require the behavior and are unambiguous; fold the new mutations into `acceptance` in the same edit that fixes the high finding.
- [severity: low] Frontmatter parse errors remap only the leading `file:line`; the secondary pointer stays frontmatter-relative — verified: duplicate `status:` at file line 3 reports `...:3 duplicate key "status" (first at line 1)` where the first is actually file line 2 (`scripts/lib/docs-graph.mjs:162` remap regex touches only the prefix). Non-blocking cosmetic; remap or drop the inner line reference.

Attacks that held: duplicate definitions same-file/cross-file and via duplicate YAML keys; TEMPLATE exemption narrowing (TEMPLATE-evil.yaml errors); stray-extension hygiene (stray.yml errors); unregistered prefixes error at all three sites (definition, claim, reference); stale-pin cascade; positive DCX-11 control (citing a sketch architecture doc errors correctly); DCX-14 presence check with honestly stated limits; effective-serves fallback in --json; `design-scope: local` claim (repo tooling touching no product architecture, consistent with the CTX precedent and the design altitude rule); zero-dep design vs the new package.json (husky is repo devDependency, scripts import only node: builtins, and the spec explicitly pre-declares the superseding path); ID-grammar boundary probing of REF_RE (long-prefix and lowercase tokens don't leak refs); definition-site exclusion vs block-scalar look-alike lines; behavior-map keys correctly treated as references. Corrected round-3 purpose citation (CTX-2, CTX-4, CTX-5 as lib importers) matches actual imports. All five round-3 fixes verified by mutation; working tree left byte-identical (checksummed before/after).
