---
kind: challenge-record
spec: .spec/specs/top-content-topics.yaml
round: 2
date: 2026-07-19
verdict: pass
by: cortex:architect-challenger (delta-scoped — verify r1 fixes + LRN-19 agenda single-source sweep)
---

# TOP spec — round 2 (delta)

All round-1 findings verified RESOLVED: GEN-1 v4 reconciliation confirmed (the
planner pairs a taxonomy type with an agenda subject; DEC-23 binds GEN-1;
G-1/G-2/G-4/G-5 pin GEN-1@4, 0 dirty); DM-13 v2 evolution fields; thin-Memory
degradation consistent with MEMS-4; the LRN-20 catch-rate + evidence-pointer
reframe; deterministic declined-topic keying; the cold-start bootstrap.
Independent LRN-19 sweep: every DIRECT agenda consumer is reconciled to the
single source (STRS-1, GEN-1@4, EXT-1@2, EXT-4@2, glossary). No surviving high.

Three one-line fixes applied in the change recording this verdict:
- TOPS-1 topic hard-deletion synced to BIL-2/SEC-4 (was "only wrong-org purge",
  contradicting its own DM-13 v2 constraint).
- TOPS-3 names declined topics as a new structured-unknown asked-set consumer
  (MEMS design-assumption (c)) — the MEMS-6 enumeration straggler, closed
  spec-side without re-opening the approved MEM spec.
- DEC-23's human-readable "reshapes" list now names GEN-1 (matching its binds).

Out of scope, noted for later: NWS-4 derives org-domain topics independently of
the TOP-4 agenda — a future single-source reconciliation to settle when the NWS
spec is authored (distinct reader-browse function), not a TOPS defect. Process:
the challenger git-checkout'd goals.yaml mid-run and self-corrected (LRN-21).

VERDICT: pass
