{
  "$schema": "https://json.schemastore.org/tsconfig",
  "//": "SSR + React-islands package: the DOM libs. Astro's own tsconfig (astro/tsconfigs/strict) joins when the renderer is built. Type-checked directly by the gate.",
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2023", "DOM", "DOM.Iterable"]
  },
  "include": ["src"]
}
