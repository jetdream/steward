{
  "$schema": "https://json.schemastore.org/tsconfig",
  "//": "Browser package (bundler resolution — Vite). Node types cover vite.config.ts and the @backend AppRouter type import. Type-checked directly by the gate.",
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "types": ["node"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx"
  },
  "include": ["src", "vite.config.ts"]
}
