{
  "$schema": "https://json.schemastore.org/tsconfig",
  "//": "Browser package: the DOM libs. React JSX config joins with the walking-skeleton increment (Vite + React). Type-checked directly by the gate.",
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2023", "DOM", "DOM.Iterable"]
  },
  "include": ["src"]
}
